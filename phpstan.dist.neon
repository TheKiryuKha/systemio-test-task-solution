parameters:
    paths:
        - bin/
        - src/

    level: max

    excludePaths:
        analyse:
            - vendor
    
    ignoreErrors:
        - '#Property .+::\$id \(int\|null\) is never assigned int so it can be removed from the property type\.$#'